/**
 * Storage keys used throughout the application.
 * Centralizing keys prevents collision and typos.
 */
export const STORAGE_KEYS = {
  AUTH: 'muse.auth',
  THEME: 'muse.theme',
  SEARCH_HISTORY: 'muse.search-history',
} as const;
